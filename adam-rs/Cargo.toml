[package]
name = "adam-rs"
version = "0.1.0"
edition = "2024"
description = "Runtime library for property model constraint graphs"

[dependencies]
slotmap = "1.1"
anyhow = "1.0"

[dev-dependencies]
cel-runtime = { path = "../cel-runtime" }

[features]
playground = []

[lints]
workspace = true
