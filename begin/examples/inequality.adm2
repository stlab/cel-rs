sheet inequality {
    cell max_v = 100 filter 0..=200;
    cell a = 0 filter 0..=max_v;
    cell b = 42 filter 0..=max_v;
    cell c = 100 filter 0..=max_v;

    relationship {
        a := min(a, b);
        b := max(a, b);
    }
    relationship {
        b := min(b, c);
        c := max(b, c);
    }
}
