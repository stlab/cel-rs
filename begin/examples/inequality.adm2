sheet inequality {
    cell a = 0.0;
    cell b = 0.0;
    cell c = 2.0;

    relate {
        a := if a < b { a } else { b };
        b := if b < a { a } else { b };
    }
    relate {
        b := if b < c { b } else { c };
        c := if c < b { b } else { c };
    }
}
