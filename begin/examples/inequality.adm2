sheet inequality {
    cell a = 0.0;
    cell b = 0.0;
    cell c = 2.0;

    relationship {
        a := min(a, b);
        b := max(a, b);
    }
    relationship {
        b := min(b, c);
        c := mac(b, c);
    }
}
