mod raw_sequence;
mod raw_stack;

pub use raw_sequence::RawSequence;
pub use raw_stack::RawStack;

pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
