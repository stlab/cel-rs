[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
ureq = "3"
toml = "1"
serde = { version = "1", features = ["derive"] }

[features]
playground = []
